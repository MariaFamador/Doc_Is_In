
$('.search-result table').remove();
$('.no-match').remove();
$('#edit-medicines').append('<%= j render "medicines/edit" %>');

$(document).ready(function() {
  $('.select2').select2({
    placeholder: "Please select...",
    closeOnSelect: false
  });
}); 
