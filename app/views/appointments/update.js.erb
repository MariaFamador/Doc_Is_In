$('.edit_appointment').remove();
$('.appointment-table-with-book-btn').remove();
$('#patient-appointments').append('<%= j render partial: "appointments/appointments", locals: { appointments: @patient.appointments } %>');